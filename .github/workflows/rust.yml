name: rust

on:
  push:
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # rustup auto-installs the toolchain pinned in rust-toolchain.toml
      # (components included) on the first cargo invocation - no setup step.
      - uses: Swatinem/rust-cache@v2
      - name: fmt
        run: cargo fmt --all --check
      - name: clippy
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: test
        run: cargo test --workspace
      # chacha20 and fips-strict are off by default, so nothing above compiles
      # them. Neither gates any code yet, which is exactly when a feature rots
      # unnoticed: the first cfg written behind one would be the first thing to
      # break it. Two invocations because the pair is mutually exclusive.
      - name: features
        run: |
          cargo clippy --workspace --all-targets --features crypto/chacha20 -- -D warnings
          cargo clippy --workspace --all-targets --features crypto/fips-strict -- -D warnings
      # The exclusion is a compile_error!, so nothing observes it unless someone
      # builds the pair it forbids - and a guard nobody fires is a guard nobody
      # notices losing. The failure has to carry the guard's own message: a build
      # broken for any other reason would otherwise read as the guard holding.
      - name: fips-strict excludes chacha20
        run: |
          if cargo build -p crypto --features fips-strict,chacha20 > "$RUNNER_TEMP/guard.log" 2>&1; then
            echo "fips-strict + chacha20 built: the compile_error guard is gone." >&2
            exit 1
          fi
          if ! grep -q "feature 'fips-strict' is incompatible with the non-FIPS 'chacha20' cipher" \
               "$RUNNER_TEMP/guard.log"; then
            echo "the build failed, but not on the guard:" >&2
            cat "$RUNNER_TEMP/guard.log" >&2
            exit 1
          fi

  deny:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: EmbarkStudios/cargo-deny-action@v2
